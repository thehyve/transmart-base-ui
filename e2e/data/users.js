'use strict';

var users = {
            'admin': { username: 'admin', password: 'admin' },
            'invalid_user': { username: 'invalid_username', password: 'invalid_userpassword' }
            }

module.exports = users;
